const amount = 9
const comparableNumber = 10

if (amount > comparableNumber) console.log(`${amount} is larger than ${comparableNumber}`)
else console.log(`${amount} is smaller than ${comparableNumber}`)

console.log("That as my first Node App! :)")